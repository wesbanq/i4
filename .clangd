# Merged with compile_commands.json / compile_flags.txt. Ensures clangd always sees
# the same -std and -I as build.ninja (cflags), even if "directory" resolution fails.
CompileFlags:
  Add:
    - -std=c++20
    - -Wall
    - -Wextra
    - -I
    - i4core
